import SectionTitle from "../../components/SectionTitle/SectionTitle";

const AddNewProject = () => {
  return (
    <section className="login section">
      <SectionTitle title="Add new project" subtitle="Add a new project" />
    </section>
  );
};

export default AddNewProject;
